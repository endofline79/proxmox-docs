# Change Log

## 2026-03-10

- Created the Proxmox markdown repository.
- Added starter structure for inventory, memories, runbooks, sessions, and changes.
- Verified SSH and API access to the Proxmox host.
- Added initial host, guest, network, storage, and convention notes from live inventory.
- Investigated LiteLLM connectivity on LXC `101` and confirmed the service is reachable.
- Added service inventory notes for the Proxmox host and the LiteLLM container.
- Documented LXC `102` as the Certbot container for `occido.net` and `*.occido.net`.
- Documented LXC `103` as a legacy SQL memory experiment and a future deletion candidate.
- Documented LXC `104` as the Audiobookshelf side project with a large pending audiobook import.
- Documented LXC `106` as the Tailscale gateway and recorded the no-direct-internet-exposure policy.
- Documented VM `100` as the vLLM host, including GPU ownership, model ports, and the one-model-at-a-time runbook.
- Documented VM `202` as an active Aria experiment and captured the current agent-design lesson.
- Added storage intent and risk notes, including the role of `pb-server`, `M2EVO1tb`, `HDD12tb01`, and `local-zfs`.
- Documented VM `204` as the Tiny Tiny RSS server and tied it to the `Aria_rss` experiment lineage.
- Added a backlog note to keep unresolved infrastructure and service work visible.
